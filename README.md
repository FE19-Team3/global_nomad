# Global Nomad

체험(액티비티) 탐색 · 예약 · 관리 서비스를 제공하는 프론트엔드 프로젝트입니다.  
Next.js App Router를 기반으로 **FSD(Feature-Sliced Design)** 구조를 채택하고,  
도메인 로직은 **DDD-lite 방식**으로 점진적으로 확장할 수 있도록 설계했습니다.

---

## 🧱 아키텍처 개요

- **Framework**: Next.js (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Architecture**: FSD + DDD-lite
- **Lint / Format**: ESLint(v9 Flat Config) + Prettier
- **Git Workflow**: Husky + lint-staged + commitlint

### 아키텍처 잡은 기준

- `app` 디렉토리는 **라우팅과 레이아웃만 담당**
- 비즈니스 로직은 `features / entities`에만 존재
- API 네이밍과 도메인 구분은 **Swagger 스펙 기준**

---

## 📁 폴더 구조

```txt
src/
├─ app/                          # Next.js App Router (라우팅 전용, 비즈니스 로직 넣지않기)
│  ├─ (auth)/                    # 인증 관련 라우트 그룹
│  │  ├─ login/                  # 로그인 페이지
│  │  │  └─ page.tsx             # UI 조합만 담당
│  │  └─ signup/                 # 회원가입 페이지
│  │     └─ page.tsx
│  │
│  ├─ (public)/                  # 로그인 없이 접근 가능한 공개 영역
│  │  ├─ main/                   # 메인 페이지
│  │  │  └─ page.tsx
│  │  └─ activities/             # 체험(액티비티) 탐색
│  │     ├─ [activityId]/        # 체험 상세
│  │     │  └─ page.tsx
│  │     └─ page.tsx             # 체험 목록
│  │
│  ├─ (mypage)/                  # 로그인 후 개인 영역
│  │  ├─ profile/                # 내 프로필 조회
│  │  │  └─ page.tsx
│  │  ├─ reservations/           # 내 예약 리스트
│  │  │  └─ page.tsx
│  │  ├─ experiences/            # 내가 등록한 체험 목록 (MyActivities)
│  │  │  └─ page.tsx
│  │  └─ booking-status/         # 체험 예약 현황 대시보드
│  │     └─ page.tsx
│  │
│  └─ layout.tsx                 # 전역 레이아웃 (Header/Footer 포함)
│
├─ api/                          # Next.js API Router (BFF 계층)
│
├─ features/                     # 사용자 행동 단위 (Use Case 중심)
│  ├─ auth/                      # 인증 관련 기능
│  │  ├─ login/
│  │  │  ├─ ui/                  # 로그인 UI
│  │  │  ├─ model/               # 상태, 훅, 폼 로직
│  │  │  └─ index.ts
│  │  └─ signup/
│  │     ├─ ui/
│  │     ├─ model/
│  │     └─ index.ts
│  │
│  ├─ activity/                  # 체험 관련 기능
│  │  ├─ list/                   # 체험 목록 (검색, 필터, 정렬)
│  │  ├─ detail/                 # 체험 상세
│  │  ├─ register/               # 체험 등록/수정
│  │  └─ reservation/            # 체험 예약
│  │
│  ├─ review/                    # 리뷰 기능
│  │  ├─ create/
│  │  └─ list/
│  │
│  ├─ notification/              # 알림 기능
│  │  ├─ list/
│  │  └─ panel/
│  │
│  └─ profile/                   # 내 정보 관리
│     └─ edit/
│
├─ entities/                     # 도메인 단위 (DDD-lite)
│  ├─ activity/                  # 체험 도메인
│  │  ├─ model/                  # Entity, Enum, 도메인 타입
│  │  └─ api/                    # 체험 관련 API 호출
│  │
│  ├─ reservation/
│  │  ├─ model/
│  │  └─ api/
│  │
│  ├─ user/
│  │  ├─ model/
│  │  └─ api/
│  │
│  └─ notification/
│     ├─ model/
│     └─ api/
│
├─ widgets/                      # 페이지 레이아웃을 구성하는 UI 블록
│  ├─ header/
│  ├─ footer/
│  ├─ side-nav/
│  └─ modal/
│
├─ shared/                       # 프로젝트 전반에서 재사용
│  ├─ ui/                        # 공통 UI 컴포넌트
│  ├─ lib/                       # 유틸 함수
│  ├─ hooks/                     # 공통 훅
│  ├─ config/                    # 환경 설정, 상수
│  └─ assets/                    # 이미지, SVG 등 정적 자산


---

##  🔐 로그인 기준 역할 분리 예시

features/auth/login
→ 사용자가 로그인하는 화면과 흐름
(입력, 버튼 클릭, 로딩/에러 처리, 성공 후 이동)

entities/user/model
→ 로그인된 사용자 개념과 타입
(User, Role, Token 구조 등 도메인 정의)

entities/user/api
→ 사용자 관련 외부 API 호출
(로그인 요청, 토큰 재발급, 내 정보 조회)

app/api/auth/login
→ 인증을 처리하는 서버 중계(BFF)
(쿠키 저장, 토큰 보호, 응답 가공)

features = 행동, entities = 개념, app/api = 보안·중계
```
